# kitty session
# 启动方式: kitty --session ~/.config/kitty/saved-session.conf

# --- Tab 1: th2tf (ssh galaxy-h20-sm90) ---
new_tab th2tf
cd ~
launch bgo ssh galaxy-h20-sm90

# --- Tab 2: bench (ssh galaxy-h20-sm90) ---
new_tab bench
cd ~
launch bgo ssh galaxy-h20-sm90

# --- Tab 3: lg (lg-torch) ---
new_tab lg
cd ~/Documents/workspace/lg-torch
launch zsh

# --- Tab 4: erdos ---
new_tab erdos
cd ~/Documents/workspace/erdos
launch zsh

# --- Tab 5: torch (libtorch) ---
new_tab torch
cd ~/Documents/workspace/libtorch
launch zsh

# 启动时聚焦到第一个 tab
focus_tab
